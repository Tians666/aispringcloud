server:
  port: 8087
spring:
  application:
    name: feign
eureka:
  client:
    serviceUrl:
      defaultZone:  http://localhost:8081/eureka/
  instance:
    prefer-ip-address: true
feign:
  hystrix:
    enabled: true
